{
  "name": "the-canon-event",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "treepo forest: Bureau of Canon Events — every universe's spider bite",
  "scripts": {
    "build": "vp pack",
    "typecheck": "tsc --noEmit",
    "check": "vp check",
    "dry-run": "pnpm build && node ../scripts/seed.ts dry-run",
    "seed": "pnpm build && node ../scripts/seed.ts seed"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@repo/utils": "workspace:*"
  },
  "devDependencies": {
    "@repo/treepo-seed-api": "workspace:*",
    "typescript": "^5.6.3",
    "vite-plus": "^0.1.19"
  }
}
